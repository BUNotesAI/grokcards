## Session 1 — 项目 Bootstrap：四关注点体系 + tauri-specta 类型安全

### 完成
- 四关注点项目管理体系落地（设计/进度/质量/日志）
- 创建 `docs/` 目录结构：`progress/`, `devlog/`, `handoff/`
- 编写 `docs/design.md` 架构文档
- 编写项目级 `CLAUDE.md`（质量规则、开发流程、构建命令）
- 引入 tauri-specta v2.0.0-rc.24 实现 TS/Rust IPC 类型安全
- 配置 specta Builder + bindings.ts 自动生成（via `cargo test export_bindings`）
- App.tsx 改用 typed `commands.greet()` 替代 raw `invoke()`
- Rust clippy 零警告，TS 编译零错误

### 关键决策
| 决策 | 原因 |
|------|------|
| 不用 hooks，用 CLAUDE.md 流程规则 | 项目初期轻量，hooks 增加复杂度 |
| tauri-specta 作为 IPC 类型安全方案 | RS 为 source of truth，编译期保障优于运行时 guard |
| bindings.ts 加入 .gitignore | 生成文件不提交，避免 merge 冲突 |
| 用 test 生成 bindings 而非 app 启动 | CI/CLI 环境无 GUI，test 更通用 |

### 遗留
- CLAUDE.md 质量体系第一版只落地 3/17，后续重写补齐至 17/17
- TodoMVC 设计已在对话中完成（rusqlite + Deep Module + 7 commands），待写 spec 文件和实现
- 项目还没有 git init

### 下次从这里开始
→ 详细交接上下文见 [docs/handoff/backend.md](../handoff/backend.md)
