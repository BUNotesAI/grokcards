# 2026-04-14 Devlog

## Session: Phase A Edge 判别联合重构完成

### 背景

承接上 session (35ea0d27) 的准备工作 —— L0 防火墙原则立项 + type-first bootstrap 流程
+ d29d6df 绷带 fix —— 本 session 把 Phase A 一路推到完成,共 4 commit 横跨 3 个子阶段。

### 路线回顾

| 子阶段 | 目标 | Commit |
|---|---|---|
| 1 — type sketch | 只放类型 + parse + 单测，零业务 impl,让用户在形状定型前 review | `90d727a` |
| (预备) rename | 给旧 `models::Edge` 结构体改名 `EdgeRow` 让出名字给新判别联合 | `cddecc4` |
| 2a — connect 强类型化 | `EntityGraph::connect(&Edge)` + `user_draw_edge` + 绷带退场 + TS 同步 | `be7ea8b` |
| 2b — reader 修复 + 数据扩展 | `note.rs`/`alias.rs` reader 穷尽 match + `GraphNote`/`CardAlias` 加 `linked_question_ids`/`linked_task_ids` + `buildEdges.ts` 渲染新链接 | `675f764` |

### 用户关键决策（sub-stage 1 review 时确认的建模）

- **Card LinkTo** → 可指向 6 类 entity
- **Note LinkTo + Note SeeAlso(Obsidian)** → Note 支持 link 到 Q/T 和 obsidian 外部文档(推翻原计划的 "NoteLinkTarget 不含 Q/T" 方案,改为 reader 穷尽 match 防御)
- **Section / Task 不主动发边** → 类型层无 SectionLink/TaskLink 变体
- **Alias 独立 edge 能力仅 AliasLink 一种** → Related/SeeAlso 等显示信息通过 `CardToAlias` 反查 owning card 继承
- **Question** 可主动 link 到其他 entity 但不支持 obsidian 外部引用
- **SeeAlso 目标是外部 Obsidian 文档** → 新增 `ObsidianLink` newtype 与 entity id 类型层隔离

### 建模简化（sub-stage 1 后期的重要 refactor）

原始方案每个 link 变体有独立的 `*LinkTarget` 子 enum。用户答复后发现四个子 enum 形状
一致,统一用 `EntityId` 作通用 target,**删掉了 5 个重复子 enum**。防火墙机制从 "不可
表达"(NoteLinkTarget 无 Q/T 变体)转为 "必须穷尽 match"(reader 处理全部 6 个 EntityId
variant,禁止 `_` 通配)。两种机制都堵住踩坑样例 1 的 silent failure 根因。

### 踩的坑

- **混淆 `section_members` 表与 `section_link` edge** — Sub-stage 1 初稿把
  `section_link` 当作 `SectionMember` edge 变体建模,实际它是 edges 表里的 section→section
  历史数据,section_members 才是独立的成员关系表。用户要求画 2D 表时发现矛盾,回头
  修正:删除 `SectionMember` 变体,`section_members` 表留给未来独立建模的 task。
  教训:"画布数据结构" 和 "DB 表结构" 不一定 1:1 对应,每次建模前先 grep 真实 SQL。

- **CardToAlias 语义一度被当成"冗余"** — 初稿建议 sub-stage 2 拿掉。用户澄清后明白
  它是 alias 继承 owning card 的 Related/SeeAlso 显示的**反查路径**,不是冗余
  (alias 没有独立的 Related/SeeAlso,靠 CardToAlias 回查 owning card)。
  doc 里已写明这个语义,避免未来误删。

- **`test_connect_with_style_and_label` 留下 style/label 参数的技术债** — Sub-stage 2a
  发现这套参数 TS 从未使用、生产代码从未设值,只有一条单测在测。整体退役,DB 列
  保留(始终 NULL),`connect` 签名不再接受 style/label。

### 测试计数

| | 变化前(commit `3d27294`) | Phase A 完成后 | Δ |
|---|---:|---:|---:|
| Rust lib tests | 198 | 211 | +13 |
| TS vitest | 209 | 209 | 0 |

新增 Rust 测试明细:
- `edge::tests::*` ×7(EntityId prefix parse)
- `edge::tests::user_draw_edge_*` ×6(6 种 from kind)
- `note::tests::test_get_note_reads_question_and_task_linked_ids`
- `note::tests::test_get_note_fails_on_unknown_target_prefix`
- `alias::tests::test_get_alias_reads_question_and_task_linked_ids`
- `alias::tests::test_get_alias_fails_on_unknown_target_prefix`

删除测试:`resolve_user_drawn_edge_type_*` ×3（绷带退场）+ `test_connect_with_style_and_label` ×1（API 退役）

### 五项预提交检查

每个 commit 前都跑过且全绿:

- `cargo clippy --workspace -- -D warnings` → clean
- `cargo test --workspace` → 211 lib passed
- `pnpm test` (vitest) → 209 passed
- `pnpm build` (tsc + vite) → 无错
- `bindings.ts` 自动 regen 为最新(entity_connect 2 参数 / entity_relate 新增 /
  GraphNote+CardAlias 新字段)

### 下次从这里开始

Phase A 完成,backend 功能域的 Active 切换到 **Phase B — 节点菜单能力类型化**。
Phase B 的起点已经在 `docs/handoff/backend.md` 原「Phase B 排队上下文」段落写好,
新 session 直接从 `NodeContextMenu.tsx` 的 5 个 variant 做交集/差集分析开始。

→ 详细交接上下文见 [docs/handoff/backend.md](../handoff/backend.md)

---

## Session: Phase B1 节点菜单 catalog 驱动渲染完成

### 背景

Phase A 提供了 Edge 判别联合 + 6 种 newtype id + `EntityId` + `user_draw_edge` 等后端对接点。
Phase B1 承接 Phase A 的防火墙思维,把"五份分散硬编码的 *MenuConfig interface"重构成
单一 catalog 驱动渲染:判别联合 + applies_to Set + ui_kind + visibility + order。

Phase A 遗留文档(session 35ea0d27 结束时写的 handoff + devlog 链接补丁)在本 session
开始时是 unstaged 状态,两次 commit 都只带了 Phase B1 自己的内容,遗留文档留到本次收尾
commit 一起处理。

### 路线回顾

| 子阶段 | 目标 | Commit |
|---|---|---|
| 1 — 盘点分析 | 读 5 个 *MenuConfig 源码,列三维能力清单,识别可疑缺口,四轮决策对话定 B1 scope | (纯对话,无 commit) |
| 2 — type sketch | 新建 `NodeCapabilityCatalog.ts`:NodeKind 5 种 + 10 Capability variant + NODE_CAPABILITIES const + per-NodeKind handlers(Pick 裁剪)+ NodeMenuConfig 判别联合 | `6a37f25` |
| 3 — 接线 | NodeContextMenu catalog 驱动渲染 + EntityNode/GraphView wiring 改新 shape + 测试文件重写 | `29d1fb8` |

### 用户关键决策(sub-stage 1 review 时四轮澄清的结果)

- **Task 预留**:Task 虽然是现有的渲染节点(`EntityKind` 含 "task"),但 Phase B1 暂不接入菜单 —— `NodeKind` union 不含 "task",类型层不埋技术债,后续独立 task 接入时只需扩 union + 几处 applies_to
- **Phase B 拆成 B1/B2**:B1 纯 TS(复用现有 Rust command),B2 扩 Card/Question/Task 的 color schema(Rust 变更)。Phase B1 结束时把 B2 放到 Next,不让纯 TS 重构和 schema 变更混在一个 PR
- **Alias 不含 edit_title**:Alias 没有独立 title,视觉上借 source card 的 title 渲染,edit 对 alias 没有语义
- **Card 不含 delete**:Card 生命周期由 CLI/agent 管理,UI 菜单暴露 delete 风险高,保持现状
- **copy_title 统一为 copy_uuid_title**:砍掉 Card 专属的 `onCopyTitle`,所有节点都是 `UUID:{id} {normalize(title)}`;normalize 方向不重复(`normalizeCardTitleForClipboard` 剥离 md vs `normalizeBoldMarkdown` 修正渲染,两者相反方向)
- **canonical order 全局统一**:所有节点菜单项按 catalog 里的 `order` 渲染,不允许 per-kind 顺序覆盖
- **Delete 标签统一**:原 "Delete alias" / "Delete section" 改为 "Delete"

### 防火墙落地

- **NodeKind union 限定**:`"card" | "alias" | "note" | "question" | "section"`,Task 不在其中 → 传 `{kind: "task"}` 给 `NodeMenuConfig` 编译失败
- **Capability variant 写死 ui_kind**:`SetColorCapability.ui_kind = "custom"` 是类型字面量,const 初始化传 `ui_kind: "plain"` 会编译失败
- **visibility 规则写入类型**:`MoveToSectionCapability.visible = "sections_not_empty"` 必须存在 / `RemoveFromGroupCapability.visible = "in_section"` 必须存在
- **destructive 写入类型**:`DeleteCapability.destructive = true` 必须存在
- **NodeCapabilityHandlerMap 单一声明源**:per-NodeKind handlers 用 `Pick<NodeCapabilityHandlerMap, ...>` 裁剪,drift 风险集中在此一处
- **渲染 dispatch 的 cast 是 sound 的**:`menu.handlers as Partial<NodeCapabilityHandlerMap>` —— 每个 Pick 子集都是 HandlerMap 的 Partial,结构子类型合法

### 真实 UX 变化

| 变化 | 影响 |
|---|---|
| Card 菜单 `Copy title` → `Copy UUID + title`(带 UUID) | 用户粘贴 Card 标题时会多出 `UUID:card_xxx ` 前缀,是用户明确要的 —— 配合 agent + CLI 定位 |
| Alias 菜单**新增** `Copy UUID + title` | 之前没有复制入口,现在 alias 可 copy `UUID:alias_xxx {source card title}` |
| Section 菜单**新增** `Copy UUID + title` | 之前 Section 菜单只有 Delete,现在多了 copy |
| Section 菜单**新增** `Set color`(7 色块) | Section 之前无色板,现在支持背景色(复用 section_update) |
| Delete 标签统一 | "Delete alias" / "Delete section" → "Delete",handler 行为不变 |

所有其他菜单项(draw_connection / related / create_alias / jump_to_source_card / edit_title / move_to_section / remove_from_group / delete / set_color(Note))**行为与旧版本完全一致**,只是组织方式从 5 份 hardcoded 变成 catalog 驱动。

### 踩的坑

无明显踩坑。一个小注意点:

- **`data.sections` 之前不在 menuHandlers useMemo 的 deps**:原代码的 copy 函数都按 kind 各查一个 data field(cards/notes/questions),不需要 sections。合并成 `onCopyEntityUuidTitle` 后,Section 的 title 要从 `data.sections` 查,deps 漏掉会导致 stale closure(section 改名后 copy 出来是旧 title)。补上 `data.sections` 到 deps。

### 建模 DRY 优化(sub-stage 3 中途的重要 refactor)

sub-stage 2 的初版 type sketch 把 per-NodeKind handlers 写成 5 个独立 interface,每个声明
`copy_uuid_title: () => void; draw_connection: () => void; ...`。sub-stage 3 写 NodeContextMenu
渲染时发现:单一渲染路径需要从 `menu.handlers` 按 `cap.kind` 动态取 handler,没有通用类型
表达"menu.handlers[cap.kind]"的签名。

refactor:引入 `NodeCapabilityHandlerMap` 作为单一声明源,per-NodeKind handlers 改为
`Pick<NodeCapabilityHandlerMap, ...>`。好处:
- 字段签名单一源头,少一份维护
- `menu.handlers as Partial<NodeCapabilityHandlerMap>` 是 sound cast(结构子类型)
- 渲染时 `handlerMap[cap.kind]` 正确推出签名(Partial 的 indexed access)

这个 refactor 同时落在 sub-stage 3 的 `29d1fb8` commit 里。

### 测试计数

| | Phase B1 前 | Phase B1 后 | Δ |
|---|---:|---:|---:|
| Rust lib tests | 211 | 211 | 0 |
| TS vitest | 209 | 215 | +6 |

新增 TS 测试明细(`NodeContextMenu.test.tsx`):
- Card 菜单不含 Delete(Card 由 CLI/agent 管理)
- Card sections 为空时不显示 Move to Section
- Alias copy_uuid_title click(新 UX)
- Section copy_uuid_title click(新 UX)
- Section set_color click(新 UX,#ffadad)
- Section 不含 draw/move/remove/edit 能力(白名单正确性)

`QuestionNode.test.tsx` 的 `makeQuestionMenu()` 工具函数改用新 shape(同步迁移)。

### 五项预提交检查

每个 commit 前都跑过且全绿:

- `cargo clippy --workspace -- -D warnings` → N/A(无 Rust 改动)
- `cargo test --workspace` → N/A
- `pnpm test` (vitest) → 215 passed(+6)
- `pnpm build` (tsc + vite) → 无错
- `bindings.ts` → N/A(无 command 签名变更)

### Code Review 6 项(sub-stage 4 收尾自查)

1. **测试覆盖** ✓ — 每个新 capability 有 presence + click assertion;visibility 规则覆盖 sections 空态和 in_section 两条;边界 case(Card 无 Delete / Section 白名单)显式断言
2. **逻辑正确性** ✓ — applicableCaps 的 filter/sort 单向、可读;groupOf 分组 separator 插入覆盖 normal/custom/destructive 三组过渡;onCopyEntityUuidTitle 对所有 7 种 EntityKind 显式处理(含 task/whiteboard 走 return)
3. **回归风险** ✓ — 209 原测试 + 6 新测试全绿;label 统一的 3 处在测试中同步更新;行为一致
4. **I/O 正确性** ✓ — 无 Rust / DB 改动
5. **IPC 类型安全** ✓ — 无新 command,无 bindings.ts 变化,`onSetSectionColor` 复用已登记的 `section_update`
6. **建模强度** ✓ — 见上面"防火墙落地"六项

### 下次从这里开始

Phase B1 完成,backend 功能域的 Active 为空。Next 有两条:

1. **Keysight Task 节点菜单接入** — 纯 TS 增量扩张,把 Task 纳入 NodeKind union,在 catalog 里扩 applies_to,为 TaskNode 装配 menuConfig。不动 Rust。
2. **Keysight 节点菜单 Phase B2** — Rust schema 变更,给 Card/Question/Task 加 color 字段,然后扩 catalog 的 set_color.applies_to。涉及 migration,风险面较大。

或者切换到其他功能域(frontend / ipc / infra)的 Next。

→ 详细交接上下文见 [docs/handoff/backend.md](../handoff/backend.md)(Phase B1 完成后已 pre-stage "Keysight Task 节点菜单接入" 作为下一 task,status=ready-to-resume,含能力清单预读结论)

---

## Session: Keysight Task 节点菜单接入完成

### 背景

承接上 session 的 pre-stage handoff(commit `3454154`):Phase B1 的 catalog 已经按判别联合组织好,
Task 作为 "EntityKind 里存在但 NodeKind 里故意缺失" 的状态预留。本 session 直接从 handoff 的
**子阶段 2(type sketch 扩张)** 开始,子阶段 1(能力清单预读)上一 session 已经做完。

纯 TS 增量扩张,零 Rust 改动。能力范围被后端现状约束到 **3 项**(见下面的「能力边界」段)。

### 路线回顾

| 子阶段 | 目标 | Commit |
|---|---|---|
| 2 — type sketch | NodeCapabilityCatalog 扩 NodeKind + 3 个 applies_to + TaskNodeHandlers + NodeMenuConfig.task + TaskMenuConfig;NodeContextMenu re-export | `18ba92a` |
| 3 — 装配接线 | TaskNode 加 3 个 menu prop + JSX;EntityNode 新 taskMenu useMemo + case "task" 传 prop;GraphView copy case "task" 从 return 改为 data.tasks.find | `2ba46c6` |
| 4 — 测试扩充 | NodeContextMenu.test.tsx 新增 variant='task' 套(8 case,含**白名单边界**锁死能力集合) | `68bde5f` |
| 5(补) — harness-check-tests 发现的缺口 | TaskNode.test.tsx +2(contextMenu prop 接线);EntityNode.test.tsx +2(kind=task dispatch + 完整菜单接线链路) | `154ceae` |

### 能力边界(为何只有 3 项)

Task 菜单在本 task 的 scope 下能做 **3 项能力**,其余由后端现状刚性约束:

| Capability | 给 Task? | 为什么 |
|---|:-:|---|
| `copy_uuid_title` | ✓ | Task 有 title,走统一 normalize pipeline |
| `move_to_section` | ✓ | `section_members` schema 通用,接受任意 entity kind |
| `remove_from_group` | ✓ | 同上 |
| `draw_connection` | ✗ | Phase A Edge 判别联合**编译期**禁 Task 作 from(`edge.rs:101-102` 无 TaskLink 变体,踩坑样例 1 的防火墙遗产) |
| `edit_title` / `delete` | ✗ | 后端无 `task_update` / `task_delete` domain 函数,需 Rust 扩张(留 Phase B2) |
| `set_color` | ✗ | Task 模型无 color 字段(`models.rs:266-279`),需 schema 扩张(留 Phase B2) |
| `related` / `create_alias` / `jump_to_source_card` | ✗ | Card/Alias 专属语义,不适用 Task |

**关键决策**:不补 minimal `task_update` command 只为让 Task 菜单多一项 `edit_title` —— 违反纯 TS 增量原则,
且应该和 `task_delete` 一起补(两者都是 Phase B2 里的 domain 扩张)。

### 防火墙落地

- **NodeKind union 只加一行**:`... | "section" | "task"`。扩展点集中,其他 capability 的 applies_to 不含 task,静默漂移不可能
- **TaskNodeHandlers 精确 Pick**:`Pick<NodeCapabilityHandlerMap, "copy_uuid_title" | "move_to_section" | "remove_from_group">`。尝试加第四个能力时,如果 NODE_CAPABILITIES 没更新 applies_to,渲染时会过滤不出来;如果 applies_to 更新了但 Pick 没扩,NodeMenuConfig.task 的 handlers 少字段编译失败
- **NodeMenuConfig 判别联合扩 task 变体**:` | { readonly kind: "task"; readonly handlers: TaskNodeHandlers }`。对应的 `Extract<NodeMenuConfig, { kind: "xxx" }>` 别名不受影响(Extract 只匹配对应 kind,新 variant 不污染现有类型)
- **白名单测试锁死 UI 边界**:`NodeContextMenu.test.tsx` 的 task variant 显式 assert 7 项不该出现的菜单项(Draw connection / Edit title / Delete / Set color / Related / Create alias / Jump to source card)—— 即使未来 handler 被错误扩,UI 渲染路径仍然被测试堵住
- **onCopyEntityUuidTitle 切 case "task" 穷尽**:原来的 `case "task": case "whiteboard": return` 绑带拆开,task 走正常 lookup(`data.tasks.find(t => t.id === entityId)?.title`),whiteboard 保持 return。switch 对所有 7 种 EntityKind 显式处理

### 踩的坑

无。本 task 范围小,依赖 Phase B1 已经建立的 catalog 基础设施,只做 additive 扩张。

**一个小提醒**:`pnpm` edit 过程中 TypeScript 诊断系统偶尔显示 stale "unused import" 警告(因为 JSX 使用还没到达 LSP 重新分析),但每次 `pnpm build` 运行完整 `tsc` 后都是 clean —— 不用追诊断提示,跑 `pnpm build` 看真相。

### 测试计数

| | 前(commit `2a79d21`) | 后(本 session) | Δ |
|---|---:|---:|---:|
| Rust lib tests | 211 | 211 | 0 |
| TS vitest | 215 | 227 | +12 |

新增 TS 测试明细:

**`NodeContextMenu.test.tsx` — variant='task' 描述套(+8)**:
- 打开菜单显示 Copy UUID + title / Move to Section
- sections 为空时不显示 Move to Section
- 未在 section 时不显示 Remove from group
- 在 section 中时显示 Remove from group
- 点击 Copy UUID + title → handler
- 点击 submenu Section A → move_to_section("sec_a")
- 点击 Remove from group → handler
- **白名单边界**(7 项 not in document)

**`TaskNode.test.tsx`(+2)**:
- contextMenu null → ⋯ 按钮 absent
- contextMenu 非 null → ⋯ 按钮 present

**`EntityNode.test.tsx`(+2)**:
- kind=task → 渲染 TaskNode
- kind=task + menuHandlers → 完整链路(dispatch → useMemo → TaskNode → NodeContextMenu)渲染 ⋯ 按钮

### 五项预提交检查

每个 commit 前都跑过且全绿:

- `cargo clippy --workspace -- -D warnings` → clean(无 Rust 改动)
- `cargo test --workspace` → N/A(无 Rust 改动)
- `pnpm test -- --run` → 223(commit 3 后)/ 227(commit 4 后)passed
- `pnpm build`(tsc + vite) → 无错
- `bindings.ts` → N/A(无 command 签名变更)

### Code Review 6 项(sub-stage 5 收尾自查)

1. **测试覆盖** ✓ — 每个新 capability 有 presence + click;visibility 覆盖 sections 空态 + in_section 两条;白名单边界 7 项显式断言;harness-check-tests 补完 TaskNode prop 接线 + EntityNode dispatch 两层缺口
2. **逻辑正确性** ✓ — catalog 扩张 additive,未破坏现有 filter/sort/dispatch;`Extract<NodeMenuConfig, { kind: "xxx" }>` 对 card/alias/note/question/section 仍正确(Extract 不受新 variant 影响)
3. **回归风险** ✓ — 215 原测试全绿,新增 12 全绿;EntityNode memo 比较保持不变(新 taskMenu 在 useMemo 内,走现有的 menuHandlers/entity deps)
4. **I/O 正确性** ✓ — 无 Rust / DB 改动
5. **IPC 类型安全** ✓ — 无新 command,无 bindings.ts 变化
6. **建模强度** ✓ — 见「防火墙落地」段;新增 Task 变体后 NodeKind union 仍精确,applies_to 只含白名单能力,TaskNodeHandlers Pick 子集确保 handler 集合和 applies_to 对齐,白名单测试锁死 UI 层

### 下次从这里开始

backend 功能域 Active 为空。Next 精简为一条:

- **Keysight 节点菜单 Phase B2** — 合并处理 Card/Question/Task color schema 扩张 + Task edit/delete domain 扩张:
  - Rust:`entities` 表加 color 列 + migration
  - Rust:`card_set_color` / `question_set_color` / `task_set_color` commands(+ 副作用矩阵登记)
  - Rust:`task_update` / `task_delete` domain + commands(+ 副作用矩阵登记)
  - TS:扩 catalog 的 `set_color` / `edit_title` / `delete` applies_to 加上对应 kind
  - TS:扩 TaskNodeHandlers 从 3 项到 5 项(含 edit_title + delete)
  - 风险面大,涉及 DB schema 变更,要独立 task 做 + 测试矩阵补齐

或者切换到其他功能域(frontend / ipc / infra)的 Next。

→ 详细交接上下文见 [docs/handoff/backend.md](../handoff/backend.md)(本 task 完成后 status=done)

---

## Session: Keysight Phase B2 完成(Task 全栈 + Card/Question color)

### 背景

承接同一 session 前段的 "Task 节点菜单接入",用户确认继续推进 Phase B2 并尽量在同一 session 完成。Phase B2 的实际 scope 比原计划大 —— 原计划以为要加 schema 迁移,但 Explore 发现 `entities.color` 列早已存在,parser 和 sync 也 kind-agnostic 处理 color,所以重点变成 "把 color 字段贯穿到 IPC 层 + 把 Task 从 DB-only 变成 file-backed + 支持嵌套 whiteboard"。

### 用户决策(建模对话阶段)

进入实施前和用户走了 3 轮建模对话,敲定关键决策:

1. **Task 的业务定位** — 项目级工作包,粒度 ≈ area;kanban 看板对应 project whiteboard;每个 project 一个 kanban,tasks 在其中被 note/alias 标注连接
2. **文件路径约定** — `whiteboard/projects/{project name}/{task_id} 【TASK】{title}.md`。project 决定目录,task_id 前缀保证 rename 稳定性
3. **"conflict" 的本质** — storage vs view 的二义性。采纳方案 A:嵌套 whiteboard,whiteboard_id 允许一个 `/`,`projects/{name}` 作为 reserved 二级命名空间
4. **同目录多类型共存** — note/card/question/task 在同一 project whiteboard 目录里混放,用 `【TASK】` / `【NOTE】` / `【QUE】` / `【ATC】` 文件名前缀**视觉**区分,frontmatter `type` 字段**系统权威**区分。这个约定 parser.rs 早就实现了(`KNOWN_TYPES = ["atomic-card","note","project-task","question"]`),sub-stage 2 只是复用
5. **TaskStatus** — enum(next/active/blocked/done,对齐现有 `STATUS_STYLES`),非自由字符串
6. **ProjectName** — Rust-side newtype 做 path-safe 校验(禁 `/ \\ : * ? " < > |`),IPC 边界用 string
7. **Task invariant** — `whiteboard_id = format!("projects/{}", project)`,构造器保证

### 路线回顾(7 commit)

| 子阶段 | 目标 | Commit |
|---|---|---|
| 1 | 给 AtomicCard / TaskEntity / QuestionEntity 加 `color` 字段(DB 已就绪,只加 struct 字段 + 读路径) | `a4290e5` |
| 2+3 合并 | `domain/task.rs` 重写 940 行 +  `derive_whiteboard_id` 识别 `projects/{name}` 二级路径(两者强绑定,不能分) | `16d91df` |
| 4a | question.rs create/update 加 color 参数(`"default"` sentinel 清空),commands/GraphView 同步 | `133e2b9` |
| 5 | commands 层 5 个新命令:card_set_color / task_create / task_update / task_delete / task_set_color;`parser.rs` 加 write_color_frontmatter helper | `d6c6f18` |
| 6+7 合并 | NodeCapabilityCatalog 扩 applies_to / Handler Pick;EntityNode/GraphView 装配;TS 测试同步 | `bf96491` |

### 防火墙落地

- **ProjectName newtype**:`new(&str)` trim + 禁 9 个非法字符(空 / 路径分隔符 / Windows 禁用),之后 `as_str()` 拼路径永远安全
- **TaskStatus 边界转换**:`parse_task_status_from_ipc` 在 command 边界用 `serde_json::from_value::<TaskStatus>` 解析,未知字符串即 reject;domain 层 `parse_task_status` 同样有 `InvalidTaskStatus` 错误路径
- **TaskCreateInput / TaskUpdateInput struct**:避免 clippy `too_many_arguments`(>7 参数触发)+ 命名参数比位置参数不易混淆
- **file path 拼接不可绕过**:`task_relative_path(project, id, title)` 是唯一入口,`sanitize_file_component` 把 title 里的 `/ \\ : * ?` 等替换为 `_`,构造的相对路径无法通过 title 注入 `../`
- **render_task_markdown / render_question_markdown 对 color 值加双引号**:hex `#ffadad` 裸写会被 YAML 解析器当行内注释 →  sync 读回 DB 时 color 变 None。note.rs 有同样的潜在 bug 但测试只用命名色(yellow/amber)没 hit,本次在 task/question render 中修掉
- **parser::write_color_frontmatter 用 serde_yaml**:serde_yaml 自动处理 hex 引号,不用手 format 字符串,天然避免引号坑
- **Edge 判别联合仍禁 Task 作 from**:Phase A 留下的防火墙,B2 未触碰,因此 task 菜单仍不含 draw_connection

### 踩坑样例

**#1 YAML hex color 被当注释 —— render 层的隐性 bug**

sub-stage 2 初版 `render_task_markdown` 和 note.rs 一样写 `color: #ffadad`,跑 `test_create_task_with_color` / `test_update_task_sets_color` 失败:写进去 color 但读回来是 None。DB 里 entities.color 也是 NULL。

根因:YAML 里 `color: #ffadad` 的 `#` 是行内注释起点,parser 看到的 value 是空。note.rs 同样 bug 但测试只用 `yellow` / `amber` 这种命名色没 hit(note.rs 的 529/569/726 几处 assert 全是 `"color: yellow"` 之类)。

修复:render 时对 color 值加双引号 `color: "#ffadad"`。sub-stage 5 进一步在 parser 加 `write_color_frontmatter` helper 走 serde_yaml 路径,避免再次踩坑。

**#2 derive_whiteboard_id 只支持 1 级路径**

sub-stage 2 在写 create tests 时发现:sync_file 调用后 tasks.whiteboard_id 不是 `projects/super-tauri` 而是 `projects`。根因是原始 `derive_whiteboard_id("whiteboard/myboard/test.md")` 取第一个 `/` 之前作为 wb_id,二级路径下 projects/super-tauri 被截断成 projects。

修复:合并 sub-stage 2 和 3,`derive_whiteboard_id` 优先识别 `projects/{name}/...` 作为二级路径。保持扁平 whiteboard 原逻辑作为 fallback。加 3 个单元测试覆盖新行为 + 退化边界(`whiteboard/projects/loose.md` → 退化成扁平 "projects")。

**#3 clippy too_many_arguments (8/7)**

task::create 一开始 8 个参数 → clippy 报 `too_many_arguments`。refactor 成 `TaskCreateInput` struct,顺带让测试可以用 `..task_create_defaults("X")` 的 spread 模式写测试,更易读。update 同样处理。

### 测试计数

| | 起点(本 session 开始) | 终点 | Δ |
|---|---:|---:|---:|
| Rust lib tests | 211 | **234** | +23 |
| TS vitest | 215 | **230** | +15 (包括 Task 菜单接入的 12 + Phase B2 的 3) |

B2 新增 Rust 测试分布:
- ProjectName 5 (valid/allows spaces/trims/empty rejected/path separators rejected)
- TaskStatus roundtrip 2
- task_relative_path 2
- render_task_markdown 2
- create 3 (happy/with color/empty title rejected)
- update 3 (rewrite/rename on title/set color)
- delete 1
- get not found 1
- query_all 2
- legacy 2
- card set_color 3
- question color 3
- sync derive_whiteboard_id 2

### Code Review 6 项

1. **测试覆盖** ✓ — task CRUD 每个路径都有 happy + edge(empty title / not found / color / rename);card set_color 三路径(set / clear / not found);question color 三路径(create / update set / update preserve)。白名单 UI 测试锁死菜单边界

2. **逻辑正确性** ✓ — `task_relative_path` 拼接正确;`desired_task_relative_path` 对无变化路径返回原路径避免无效 rename;`update` 函数严格不允许改 project(current_project 从 DB 读,user 传的 project 字段被忽略);TaskCreateInput/TaskUpdateInput spread 模式在测试里验证各 field 被正确 threaded

3. **回归风险** ✓ — Rust 228(sub-stage 1 开始时,含 Task 菜单接入测试)→ 234;TS 227 → 230。现有测试除语法迁移(question 加 color 参数 → 所有 caller 加 null)和 task variant 白名单边界调整外未动

4. **I/O 正确性** ✓ — task create 走完整 write_file → sync_file → get 链路;update 处理 rename 时删旧文件 + 清 file_mtimes;delete 走 sync::remove_file 级联;card set_color 同时更新文件 frontmatter 和 entities.color 列(避免漂移)

5. **IPC 类型安全** ✓ — 所有 5 个新 command 都有 `#[specta::specta]`;bindings.ts 已 regen;`TaskStatus` 在 models.rs 有 serde + specta derive;TS 侧 `taskCreate(project, title, content, status, area, color)` 签名来自 Rust 类型;GraphView 所有 command 调用都走 `commands.*` 不用 raw invoke

6. **建模强度** ✓ — 见"防火墙落地"段。额外:`task_update` 对 color 使用 `"default"` sentinel 和 note/question 一致,前端 UI 不用学两套清空方式

### 未接入的能力(明确推迟到独立 task)

1. **Task 的 `edit_title` 能力** —— TaskNode 当前没有 inline editor(NoteNode/QuestionNode 有),接入 edit_title 需要先扩 TaskNode UI。独立 task。

2. **前端创建 Task 的入口** —— `task_create` 命令已就绪,但用户没有 UI 可以点。需要决定入口形态(右键 empty canvas / 工具栏按钮 / 快捷键),独立 task 设计。

3. **Project 目录自动创建** —— MockVaultFs 测试里直接 write_file 不需要 mkdir,但 RealVaultFs 在真实文件系统可能需要 mkdir -p。sub-stage 2 没显式处理,依赖 RealVaultFs::write_file 内部实现。如果真实环境遇到 "No such file or directory",再补。留给 sub-stage 5 集成测试暴露。

### 下次从这里开始

backend 功能域 Active 为空。Next 是 UI 层面的独立 task("Task 前端 UI 入口 + edit_title inline editor"),依赖本 session 落地的 Rust 基础设施。

→ 详细交接上下文见 [docs/handoff/backend.md](../handoff/backend.md)(B2 完成后 status=done)

---

## Session 尾声:Phase B2 post-review P0+P1 修复 + 下次 4 task 预置

B2 ship 后做了一轮三 agent 深度 review(code-reviewer / type-design / silent-failure),发现 2 个 P0 数据安全 bug 和 6 个 P1 沉默错误。全部在同 session 修完,1 commit `874ae95`。

**Review 发现的 P0/P1 修复**:
- **P0-1** `task::update` 不识别 `"default"` sentinel(实现缺少 `Some("default") => None` 分支 → `task_set_color("default")` 写字面字符串到 DB/file)
- **P0-2** `parser::write_frontmatter` / `write_color_frontmatter` 在 YAML 解析失败时 `unwrap_or_default()` 静默返回空 mapping → 下次写回销毁所有其他 frontmatter 字段 → 数据丢失
- **P1-3** `task::update` `parse_task_status.unwrap_or(Next)` 静默重写 legacy status("wip" → "next" 永久丢失)
- **P1-4** `by_status` `.filter_map(|r| r.ok())` 吞行错误
- **P1-5** `task::delete` legacy 分支漏级联 entity_tags / edges / entities_fts / section_members
- **P1-6** `card::set_color` 绕过 `sync::sync_file`,file_mtimes 不更新导致下次 sync_vault 会覆盖
- **P1-7** `derive_whiteboard_id` 对 `whiteboard/projects/loose.md` 返回 `"projects"` 作为影子 whiteboard
- **P1-8** `ProjectName::new` silently trim + 缺 NUL / 控制字符 / dots-only / leading dot / Windows reserved / 长度上限校验

测试从 234 → **245** (+11 回归测试锁住每个 bug)。

**P2 推迟到 Phase B3**(下下次独立 session):
- `enum ColorUpdate { Keep, Clear, Set(_) }` 替换 5 模块共享的 `"default"` 字符串 sentinel
- `HexColor` / `WhiteboardId` / `TaskEntity.status → TaskStatus` 等 newtype 改造

**下次 session 的 4 个 task(按 P0→P3 顺序)**:
1. **list_whiteboards 递归 `projects/*`**(纯 Rust,P0,解除"project whiteboard 不显示"的 blocker)
2. **新建 Task UI 入口**(纯 TS,P0,toolbar 按钮 + 简易 modal,依赖 Task 1)
3. **Task edit_title inline editor**(纯 TS,P1,mirror QuestionNode 模式,catalog 扩 applies_to)
4. **Kanban view**(P3,大 feature,建议先 brainstorm 5 个决策点再独立 session 做)

→ 4 个 task 的具体动作清单见 [docs/handoff/backend.md](../handoff/backend.md)(status: ready-to-resume)

---

## Session 6 — handoff 4 task 收尾(3/4 落地)+ P0 bug 调查交接

接 Session 尾声预置的 handoff,按 P0→P3 顺序落了前 3 个 task。最后测 Task 4 时**顺手发现 P0 bug**(project 白板 set_color/draw_connection 失效),走 collab 文档交接而不是当场修。

### 完成

1. **P0 Task 1 — list_whiteboards 递归 projects/* 嵌套白板** (`c91f7f7`)
   - VaultFs 加 default trait method `list_project_whiteboards`,委托 `list_first_level_dirs("whiteboard/projects")` —— Real/Mock 零额外 impl
   - `domain/overview.rs::list_whiteboards` 过滤裸 `"projects"` + chain 嵌套 project 白板(带 `projects/` 前缀)
   - 修 2 个缺口:裸 "projects" 假白板 + 空 project 目录不显示
   - Rust 245→**248** (+3 测试)

2. **P0 Task 2 — GraphToolbar 加 Task 创建按钮(project 白板专属)** (`c59cf7e`)
   - GraphToolbar 加 6 个 task-* props + 条件渲染:仅在 `currentWhiteboardId.startsWith("projects/")` 显示
   - GraphView `handleSubmitCreateTask`:project 派生 + 防御 re-check + `commands.taskCreate(project, title, null, "next", null, null)` → `layoutSetPosition` → `onSelectEntity` → invalidate
   - 4 个互斥 state 清理(Note/Question/Whiteboard ↔ Task 双向)
   - TS 230→**234** (+4 测试)
   - **dev 端到端验证通过**(用户截图: `projects/super-tauri` 创建 task "task 1 - sandbox")

3. **P1 Task 4 — TaskNode 接入 edit_title inline editor** (`7f3e24d`)
   - NodeCapabilityCatalog `edit_title.applies_to` 加 `task` + `TaskNodeHandlers` Pick 5→6 项
   - TaskNode mirror QuestionNode 的 inline editor 模式(useState/useRef/2 useEffect/`<input>`↔`<h3>` 条件渲染)
   - EntityNode `NodeContextMenuHandlers` 加 `onEditTaskTitle` + `EditingField` union 加 `task-title` + taskMenu 加 `edit_title` handler + case task 渲染传 4 prop
   - GraphView 本地 `EditingField` union 同步 + `handleCommitEdit` 加 case + invalidate `["tasks", currentWhiteboardId]` + menuHandlers 加 `onEditTaskTitle`
   - TS 234→**238** (+4 测试)
   - **dev 端到端验证通过**(同时**顺手发现 P0 bug**)

4. **P0 bug 调查交接** (`5d42b2f`)
   - 用户在 `projects/super-tauri` 白板上发现:Note/Question/Task 三种节点的 ⋯ 菜单 set_color **色块出现但点击背景色不变**;Note/Question 的 Draw connection **完全没动静**;edit_title 全部正常;wb_root 上同操作完全正常
   - 写 `docs/collaboration/2026-04-14-project-whiteboard-set-color-and-draw-connection-broken.md`(12 节调查报告):
     - 已排除 6 个假设(菜单层 wb-agnostic、applies_to 配置正确、menuHandlers 齐全、TaskNode 不读 color 但解释不了 Note/Question、handleSelectEntity 无 wb_id 参与、drawingState 无 wb_id 影响)
     - 开放 6 个假设按可能性排,优先 H1: Rust 写回路径 wb_id 派生在 `/` 出现时退化(`domain::note`/`question` 文件路径不识别嵌套 `projects/{name}`);其次 H4: `entity_connect` enum 解析失败
     - 推荐排查顺序:DevTools console → 磁盘+DB diff → git bisect → TDD 修复
     - 一句话总结: "三种节点同时挂掉、edit_title 单独 OK、wb_root 完全正常 → 指向数据写回路径里 wb_id 派生在 `/` 出现时退化或失效。**不要先看前端**"
   - handoff/backend.md 完整重写指向 P0 bug;progress Next 重排(P0 bug → P2 TaskNode color 渲染缺口 → P3 Kanban view)

### 关键决策

| 决策 | 原因 |
|------|------|
| Task 2 选 Option C(条件渲染 Task 按钮 = 只在 project 白板显示)而非 handoff 原推荐的 modal | 对齐 Whiteboard-on-root 先例,UX 一致性,避免引入新 modal 模式;project 从 currentWhiteboardId 派生 |
| VaultFs::list_project_whiteboards 用 default trait method | 委托 list_first_level_dirs,Real/Mock 零额外 impl,语义清晰 |
| `handleSubmitCreateTask` 防御性 re-check `projects/` 前缀 | UI 已条件渲染,但加防御失败 loud(console.error)而非静默 return,符合 L0 anti-silent-failure |
| 写 collab 调查文档而不是当场修 P0 bug | 用户明确要求交接给其他 agent 审查;P0 修复需 TDD + 人工 Red/Green + dev 验证,session 末尾不适合启动 |
| 4 个 task commit 独立成 commit + 各自带回归测试 | 便于 review/bisect/回滚 |

### 待续 — P0 bug 修复 unstaged 中

session 写完 collab 文档 + 提交 doc 后,git status 显示**大量 unstaged 变更**(疑似并行 agent 或用户手动跑修复):
- 5 个 Rust 文件:`commands.rs` / `domain/note.rs` / `domain/question.rs` / `domain/sync.rs` / `models.rs`
- 多个 TS 组件:`buildEdges.ts` / `edgePath.ts` / `NoteNode.tsx` / `QuestionNode.tsx` / `TaskNode.tsx` / `GraphView.tsx`
- 多个测试文件
- 新文档: `docs/collaboration/2026-04-14-project-whiteboard-set-color-and-draw-connection-fix.md`

`buildEdges` 加了 `questions` 第 5 参数(原 4 参数);新 fix 文档已确认根因不是单一 wb_id 派生,而是**多个并存的根因叠加**:
- (1) NoteNode/QuestionNode/TaskNode 背景色写死,即便 Rust 写入 color 也不渲染
- (2) `note::sync_links_to_file` / `render_note_markdown` 不会写回 question/task target → `entity_connect(Note→Question/Task)` 后 `sync_file` 把出边删回去 → "点了没动静"
- (3) `render_note_markdown` 的 hex color 没加引号,YAML 把 `#fff8b3` 当注释
- (4) Question link read/write/render 链路缺失(QuestionEntity 没 linked ids 字段、buildEdges 不接受 questions、commands TODO)

**但 GraphView.tsx:735 现有 TS 错误 "Expected 5 arguments, but got 4"** — 修复中途 stale 或某个 call site 没同步更新。**unstaged 状态不健全**,**没有自作主张提交**。

### 下次从这里开始

**第一件事**: 验证 unstaged 变更的当前状态
1. `git status` 看完整 diff list
2. 读新 collab 文档 `docs/collaboration/2026-04-14-...-fix.md` 全文,理解修复方案的范围和子步骤
3. 跑 `pnpm build` 看 GraphView.tsx:735 错误是 stale 还是真的(grep 显示该行已经是 5 args,可能是 TS server stale)
4. 跑 `cargo clippy --workspace -- -D warnings` + `cargo test --workspace`
5. 跑 `pnpm test -- --run` 看 TS 测试现状

**第二件事**: 决定 unstaged 处理策略
- 如果是别的 agent 的 work in progress → 联系用户确认是否应该 commit 还是 stash
- 如果编译失败 → 修最后那个 `Expected 5 arguments` 错误后再决定
- 如果全绿 → 用户确认后按修复方案的子阶段切 commit(每个根因独立 commit)

**第三件事**: 推进 P0 bug 修复完成 + dev 手动验证(必须实测三种 entity 的 set_color 和 draw_connection 在 project 白板上都生效)

→ 详细上下文见:
- `docs/collaboration/2026-04-14-project-whiteboard-set-color-and-draw-connection-broken.md`(本 session 调查报告)
- `docs/collaboration/2026-04-14-project-whiteboard-set-color-and-draw-connection-fix.md`(并行修复方案,unstaged)
- `docs/handoff/backend.md`(handoff 重写,status=ready-to-resume,指向 P0 bug)

Commits: `c91f7f7` → `c59cf7e` → `7f3e24d` → `5d42b2f`

---

## Session 7: P0 bug 修复全程闭环

### 背景

承接 Session 6 末尾留下的 unstaged P0 修复(并行 agent 写入但未提交)。本 session 一开始 git status 显示 5 Rust + 6 TS 组件 + 5 测试文件 + 新 collab fix 文档全部未 staged,GraphView.tsx:735 报"Expected 5 arguments"(实为 stale TS server 缓存)。

### 健康检查 → 修复 clippy → 提交 → dev 验证

1. **健康检查** — `pnpm test` (243→246) / `pnpm build` (green, GraphView 错误确实是 stale) / `cargo test` (252/252, 0 failed) / `cargo clippy` ❌ 2 errors
2. **clippy 收敛**(用户并行处理):
   - `render_note_markdown` 9 args → `NoteRenderInputs` struct
   - `parse_question_targets` 5-tuple → `QuestionLinkTargets` struct
   - `vault_fs.rs` 3 个 collapsible_if
   - 顺手补 3 类回归测试: GraphView 级连线交互 / NoteQuestion edgePath 集成 / projects 白板场景
3. **commit 拆分**(对齐 c91f7f7/5d42b2f 模式):
   - `ca18317` fix(keysight): P0 四根因修复 + 测试 + clippy 收敛 — 19 files / +687 / -116
   - `7e1a1c0` docs(backend): 修复方案文档 + Session 7 devlog
4. **dev 手动验证** — 用户在 `projects/super-tauri` 实测三种 entity 的 set_color + draw_connection 全部生效

### 四根因总结(防火墙复盘)

全是 stringly typed 漂移导致的:

| 根因 | 漂移点 | 修复 |
|---|---|---|
| 1. 节点忽略 color | NoteNode/QuestionNode/TaskNode 背景色 hardcode | 三个组件读 entity.color → 同时收口原 P2 缺口 |
| 2. sync 删 entity_connect 写入 | sync_links_to_file 没解析 question/task target | render/sync 链路加 question/task linked ids 字段 |
| 3. YAML 吞 hex color | render_note_markdown hex 没加引号 | frontmatter color 加双引号 |
| 4. Question link 链路缺失 | QuestionEntity 模型 + buildEdges + commands 全缺 | 模型补字段 + buildEdges 加 questions 第 5 参数 + commands 接线 |

修复策略全部是把出错可能性 close 在类型层(struct + frontmatter quote + 字段补齐),没有一处加 runtime 检查或 `#[allow]`。

### 测试 / 进度 / handoff 收口

- 测试: Rust 248→**252** / TS 243→**246**
- progress: P0 + P2 同时移到 Done(P2 被根因 1 顺带收口)
- handoff: status=`idle`,本 area 无 Active,只剩 P3 Kanban backlog
- changelog: ✅ 已追加

### 下次从这里开始

backend area **没有 Active 任务**,可选方向:

- **P3 Kanban view** — 大 feature,需独立 session brainstorm(用 `superpowers:brainstorming` skill)
- **Phase B3 P2 推迟项** — 见 handoff 末尾,可见缝插针挑一个(`WhiteboardId` newtype / `ColorUpdate` enum / `HexColor` newtype 等)
- **其他 area** — 看 `docs/progress/{frontend|ipc|infra}.md` 有没有 Active

Commits: `ca18317` → `7e1a1c0`
