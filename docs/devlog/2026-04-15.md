# 2026-04-15 Devlog

## Session: P3 Kanban V1 收口完成 + V1.1 Subtask 需求立项

### 背景

承接上 session (059fee08) 的 P3 Kanban view 执行(Phase 0-5 27/31 task 完成,详见 [2026-04-14.md](2026-04-14.md) Session 2)。本 session 执行最后 4 个 task —— 两个类型安全修复 + Code Review + 用户手动 walkthrough + 收口文档,然后用户临时提出 V1.1 新需求(Subtask + 双击编辑 modal),独立立项。

### 本 session 动作清单

1. `/harness-resume-context` → backend handoff → 状态与 handoff 完全吻合(Rust 264 / TS 289 / git HEAD a25c319 / 2 pre-existing untracked codex docs)
2. **动作 1** `5b1d8af` —— `compute_position_below_bottommost` doc comment 加 `TODO(B3 P2)` 显式标记 `whiteboard_id: &str` stringly-typed 债务,推迟到 Phase B3 P2 的 `WhiteboardId` newtype 重构。Finding 2 无风险修复先行
3. **动作 2** `4a1c782` —— `TaskEntity.status: String → TaskStatus` 端到端。补齐 Task 0.1 遗漏(输入参数已升 enum 但输出 DTO 仍是 String)。Finding 1 主修复
4. **动作 3 Code Review 6 项检查** 全绿(详见下)
5. **动作 4 dev walkthrough** —— 用户手动确认 6/7 场景,场景 6 澄清为 toolbar 按钮(非右键),单项目模式下显示
6. **动作 5 P3 V1 收口**(本提交)—— `docs/progress/backend.md` P3 Next → Done + `docs/devlog/2026-04-15.md` 追加 + slipbox changelog + commit

### Finding 1 的关键技术决策

Handoff 原 plan 说"SQL mapper 通过 serde lowercase rename 无缝 deserialize,无需改 SQL",但 **rusqlite 用自己的 `FromSql` trait 而不是 serde**,`r.get::<_, TaskStatus>(col)` 需要显式 impl。解决方案:

1. **`impl FromSql for TaskStatus`** —— 内部 delegate 到现有 `parse_task_status(&str)`,非法字符串装进 `FromSqlError::Other(Box<KeysightError>)`
2. **`impl ToSql for TaskStatus`** —— 内部 delegate 到 `task_status_to_str`,返回 `ToSqlOutput::Borrowed(ValueRef::Text(...))` (Copy 类型零分配)
3. **`extract_keysight_err(e: rusqlite::Error) -> KeysightError`** helper —— 对 `FromSqlConversionFailure` 做 downcast,还原装进 box 的 `InvalidTaskStatus`,回到 KeysightError enum 变体

**P1-3 回归测试 `test_update_task_rejects_invalid_legacy_status` 保持绿色** —— legacy "wip" 脏数据从 `get()` 读取路径直接 loud fail(而非原来在 `update()` 内部 parse 失败),语义等价 + 防御更早。

`domain/task::update` 原来的 `let current_status = parse_task_status(&current.status)?;` 调用点消失 —— `current.status` 已是 TaskStatus(Copy),直接 `input.status.unwrap_or(current.status)`。

### Code Review 6 项结果

| # | 项 | 结果 |
|---|---|---|
| 1 | 测试覆盖 | ✓ Rust 264 / TS 289,happy + error path 全覆盖 |
| 2 | 逻辑正确性 | ✓ compute_position 空白板 (0,0) 有测,query_kanban 5-status SQL 一致,CreateTaskModal stale state 用 conditional render |
| 3 | 回归风险 | ✓ parseDragEnd 4 case pure function test,Record<TaskStatus,...> 穷尽,加 variant 编译期失败 |
| 4 | I/O 正确性 | ✓ task::create 先 sync_file 再 compute_position,INSERT OR REPLACE 幂等,file_mtimes 由 sync_file 更新 |
| 5 | IPC 类型安全 | ✓ TaskEntity.status 全链路 enum,bindings literal union,无 raw invoke |
| 6 | 建模强度 | ✓ ProjectName newtype,TaskStatus FromSql/ToSql 闭合 SQL 边界,parseDragEnd 显式 5 比较(无 `_`),parse-don't-validate |

两个 pre-existing observations(非阻塞,非 P3 引入):

1. `TaskNode.tsx:36 STATUS_STYLES: Record<string, string>` 缺 `inbox` key,Phase 1 新增 Inbox variant 时漏补
2. `CreateTaskModal.tsx` `FormEventHandler` 在 React 19.2+ deprecated(LSP warning)

两者留作 Next,优先级低于 V1.1 Subtask 需求。

### V1.1 Subtask 需求立项

用户在 V1 收口时提出:
- **task 粒度 = area**(不是单个 actionable TODO)
- **body = GFM checklist**(`- [ ]` / `- [x]`)
- **checklist 每项对标子任务**,应在 Kanban 卡片上可见 + 双击编辑时可操作

架构决策(用户已确认采纳):

1. **Parser 放 Rust** —— `TaskEntity` 加 `subtasks: Vec<Subtask>` 字段,`domain::task::get/query_all/query_kanban` 读路径 parse body。TS 只读不 parse(L0 硬约束:业务逻辑归 Rust)
2. **Subtask 结构** —— `{ text: String, done: bool, line_index: usize }`,只认 `- [ ]` / `- [x]` 顶层 GFM checklist。嵌套留 V1.2
3. **Write path** —— 新 command `task_update_subtasks(task_id, Vec<Subtask>)`,Rust 负责重写 markdown body(TS 不构造持久化格式)
4. **卡片显示** —— 只显示 progress `done/total` 徽章或细进度条,不展开 items(展开会让列过拥挤)。详情在 double-click modal 里
5. **Modal 内容** —— title input + content textarea + checklist 编辑器(增/减/勾/改文本)+ status / area / color 保持现有。project 只读

V1.1 子 task 拆成 7 步(详见 TaskList #10-#16):
1. Rust Subtask struct + parse_task_checklist + tests (TDD)
2. TaskEntity.subtasks 字段 + 三 reader 填充 + bindings 重生成
3. task_update_subtasks command + render_checklist_back_to_body roundtrip tests
4. TS KanbanCard progress 徽章
5. TS TaskEditModal 组件
6. TS 双击接线(onDoubleClick → open modal → taskUpdate + taskUpdateSubtasks)
7. V1.1 docs 收口 + commit

### Metrics 过 session 对比

- Rust: **252 → 264**(+12,含本 session `+0`,Finding 1 修复未加新测试,只改断言)
- TS: **246 → 289**(+43,全部在上 session)
- Test files: **29 → 36**(+7)
- Build: clippy clean / pnpm build clean

### Commits 本 session

- `5b1d8af` docs(keysight): compute_position_below_bottommost 加 TODO(B3 P2) 标记 (Phase 5)
- `4a1c782` refactor(keysight): TaskEntity.status String → TaskStatus (Phase 5)
- (V1 收口 commit 待本文档写完后一并提交)

### 下次从这里开始

**立即进入 P3 Phase 6 V1.1** —— 按 TaskList #10-#16 顺序执行。第一步 TDD Red: 写 `parse_task_checklist` 的单元测试(happy path + 多种格式 + 错误 fallback),确认红色,再写实现。

→ 跨 session 精准接续见 [`docs/handoff/backend.md`](../handoff/backend.md)(V1 收口 commit 落地后会同步更新 handoff 至 V1.1 Phase 6.1 起点)。
